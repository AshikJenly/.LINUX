sudo nano /etc/anacrontab

step - 1
set path
 PATH=path/where/the_scripts/are;
 
 
 edit (these replace cron's entries)
 <per day> <minutes after boots> <name of job> <script.sh>
 
 
 note: the script must be inside the path given in PATH
